// Note: Popups are rendered via MapLibre's Popup API in MapView.tsx
// This file holds the popup CSS that MapLibre injects into the DOM.
// If we need React-rendered popups in the future, implement them here.

export {};
